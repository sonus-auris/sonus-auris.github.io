name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  ASTRO_TELEMETRY_DISABLED: "1"
  SONUS_AURIS_BUILD_ENVIRONMENT: production
  SONUS_AURIS_SITE_URL: https://sonusauris.app
  SONUS_AURIS_SITE_BASE: /

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Enforce the canonical production source branch
        shell: bash
        run: |
          set -euo pipefail
          if [[ "$GITHUB_REF" != 'refs/heads/main' ]]; then
            echo "Production Pages deployment is restricted to refs/heads/main; received $GITHUB_REF." >&2
            exit 1
          fi

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      # Astro inlines these values into generated markup. This is the only
      # production framework build: every gate below inspects this `dist/`, and
      # the exact same tree is sealed and uploaded to Pages.
      - name: Build and verify the exact production-root static site
        run: npm run check
        env:
          # Alpha desktop builds (DesktopDownloads.astro)
          PUBLIC_RUST_DESKTOP_DOWNLOAD_BASE_URL: ${{ vars.PUBLIC_RUST_DESKTOP_DOWNLOAD_BASE_URL }}
          PUBLIC_FLUTTER_DESKTOP_DOWNLOAD_BASE_URL: ${{ vars.PUBLIC_FLUTTER_DESKTOP_DOWNLOAD_BASE_URL }}
          PUBLIC_DESKTOP_DOWNLOAD_BASE_URL: ${{ vars.PUBLIC_DESKTOP_DOWNLOAD_BASE_URL }}
          PUBLIC_DESKTOP_TESTERS_URL: ${{ vars.PUBLIC_DESKTOP_TESTERS_URL }}
          # Mobile store listings (StoreButtons.astro)
          PUBLIC_APP_STORE_URL: ${{ vars.PUBLIC_APP_STORE_URL }}
          PUBLIC_PLAY_STORE_URL: ${{ vars.PUBLIC_PLAY_STORE_URL }}
          # Stable per-OS installers (StoreButtons.astro)
          PUBLIC_DOWNLOAD_BASE_URL: ${{ vars.PUBLIC_DOWNLOAD_BASE_URL }}
          PUBLIC_DOWNLOAD_WINDOWS_URL: ${{ vars.PUBLIC_DOWNLOAD_WINDOWS_URL }}
          PUBLIC_DOWNLOAD_MACOS_URL: ${{ vars.PUBLIC_DOWNLOAD_MACOS_URL }}
          PUBLIC_DOWNLOAD_LINUX_URL: ${{ vars.PUBLIC_DOWNLOAD_LINUX_URL }}

      - name: Seal the exact verified Pages tree
        run: npm run evidence:pages

      - name: Retain Pages tree and archive evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: pages-build-evidence-${{ github.sha }}-${{ github.run_attempt }}
          path: |
            artifacts/pages-evidence/
            dist/deployment.json
            dist/.well-known/security.txt
            dist/robots.txt
            dist/sitemap.xml
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 30

      # deploy-pages expects an Actions artifact named `github-pages` whose
      # payload contains artifact.tar. Upload the tar created from the verified
      # tree directly: the official upload-pages composite currently excludes
      # top-level dot-directories and would omit .well-known/security.txt.
      - name: Upload the exact verified Pages artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: github-pages
          path: ${{ runner.temp }}/artifact.tar
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  deploy:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy the verified artifact to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4.0.5
